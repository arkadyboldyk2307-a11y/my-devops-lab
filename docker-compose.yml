services:
  web:
    # ЗАМЕНИ 'arkadyboldyk' на свой логин, если он другой
    image: arkadyboldyk/my-devops-app:latest
    ports:
      - "5000:5000"
    depends_on:
      - redis
  redis:
    image: "redis:alpine"